spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ainote_auth
    username: ainote
    password: ${DB_PASSWORD}
  data:
    redis:
      host: redis
      port: 6379
app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000
  internal:
    token: ${INTERNAL_TOKEN}
